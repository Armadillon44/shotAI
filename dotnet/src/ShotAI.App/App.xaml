<Application x:Class="ShotAI.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
  <!--
    The shared control styles, which merge the fixed colours. ThemeManager.ApplyInitial adds the
    theme dictionary after them at startup step 8 and replaces it in that slot on every change
    (spec 06 7.5); every theme key is read with DynamicResource (INV-HOME-25).
  -->
  <Application.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Themes/Controls.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Application.Resources>
</Application>
