<Project Sdk="Microsoft.NET.Sdk">
  <!--
    Tests of ShotAI.Platform that need Windows: junctions, reparse tags, real sharing
    violations. The project builds on Linux like the rest of the solution; only a Windows
    runner can run it (docs/native/ARCHITECTURE.md 2.1).
  -->
  <PropertyGroup>
    <TargetFramework>$(ShotAIWindowsTfm)</TargetFramework>
    <SupportedOSPlatformVersion>$(ShotAIMinWindows)</SupportedOSPlatformVersion>
    <!-- xunit.v3 test projects are executables. -->
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
    <RootNamespace>ShotAI.Platform.Tests</RootNamespace>
    <!-- LibraryImport generates marshalling code, which needs unsafe blocks (Startup/DllSearchTests). -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <!-- Per-Monitor V2, as the app runs (spec 02 INV-CAP-26); see Support/InputHookCollection.cs. -->
    <ApplicationManifest>app.manifest</ApplicationManifest>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="xunit.v3" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\ShotAI.Platform\ShotAI.Platform.csproj" />
    <ProjectReference Include="..\..\src\ShotAI.Core\ShotAI.Core.csproj" />
  </ItemGroup>
</Project>
