// Preload script — runs sandboxed (contextIsolation: true, nodeIntegration: false).
// A typed `contextBridge` IPC surface is added in Phase 0, step 4.
// https://www.electronjs.org/docs/latest/tutorial/process-model#preload-scripts

export {};
