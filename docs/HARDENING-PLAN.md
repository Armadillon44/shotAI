# shotAI — Hardening & Cleanup Plan

Implementation plan for **all** structure-review + security-review findings (both run 2026-06-30 via adversarial multi-agent workflows, then a third workflow that produced this dependency-ordered plan and red-teamed the sequencing). Full finding detail lives in the review backlog; this doc is the **execution spec**.

**Threat model:** shotAI is a *local single-user Windows desktop tool*. Primary assets: (1) the Anthropic **API key** (main-process-only), (2) the **redaction guarantee** (blur/crop must be baked into the `flattened` PNG before any send to Claude or any export).

## Conventions
- **Branch off `main`** per track; keep separate from unrelated work.
- **Verification gates (every phase):** `npx tsc --noEmit` + `npm run lint` clean (only the pre-existing `CaptureController` non-null-assertion warning is allowed).
- **Packaging-affecting phases** (P3 sandbox switch, P5 extraResource, P9 rename): also `npm run make`/`npm run package` or at least `npm run build`.
- **Security-critical / runtime-heavy phases:** live `npm start` test of the specific scenario, **plus** direct-call/unit tests where the real UI can't reach the fixed code (see Testing infrastructure below).
- One commit per phase (bisectable); PR granularity per the table.

## Testing infrastructure (prerequisite — "P0.5")
The repo has **no test runner** (`package.json` has no `test` script; no vitest/jest). Several fixes below (S3 merge-invalidation, atomic-write serialization, the extracted pure functions) are **unreachable through the real UI** and need direct-call tests. Add a minimal **vitest** dev-dep + `test` script before P1/P2 so the security-critical fixes are actually verifiable. Pure functions to cover: `confinePath`, `resolveSendableRender`, `applyPatchAndInvalidate`, `parseRect`/`parsePoint`, `capture-geometry` (`unionRect`/`cropToRegion`/`clickBox`/`captureModeFor` golden values), `markerColorFor`.

## Execution DAG
```
P0 ─┬─> P1 ─> P2 ─┬─────────────> P6 ─┐
    │             │                   ├─> P9  (rename — LAST)
    │   P4 ─> P3 ─┘ ─> P5 ───────────┘
    │   P4 ─> P7
    └─> P8                  (P8 independent)
```
**Shipping-value order:** security track `P0 → P1 → P2 → P3 → P5`; structure track `P4 → P6 → P7 → P8 → P9`. P0/P4/P8 are low-risk and interleave.

---

## P0 — Quick-win cleanup `[T3c, T3d, T3e, T3f, DOC]` — risk: low
Land all zero-behavioral-risk tidy fixes so later phases start clean.
- **T3d** delete dead `shapeParams()` (`ClaudeService.ts:104-107`; remove `ModelParams` from its import) + `isAnnotation()` (`annotations.ts:184-186`). Grep-confirm zero importers.
- **T3c** delete private `clickMarkerRadius` (`flatten.ts:172-175`), `import { clickMarkerRadius } from './annotations'` (no cycle — annotations doesn't import flatten).
- **T3e** fix `selftest.ts` — **BOTH** broken assertions (folders are UUID-named now, `ProjectStore.ts:148,153`): line 57 `basename==='Self Test Project'` → UUID-shape regex; line 61 `sanitizedName==='Flow AB - C'` → assert `manifest.title` instead (or drop). ⚠️ Running `SHOTAI_SELFTEST=1` IS a live run that mutates real `projectsDir`/recents (restores in `finally`) — treat as live-verified, confirm it prints PASS.
- **T3f** reword stale `export.ts:17` comment ("mirror ProjectStore" — no longer true).
- **DOC** rewrite `README.md`: drop "Phase 0", remove nonexistent `scripts/postinstall-electron.mjs` ref, move shipped deps out of "Planned", document `overlay/` + `native/`. **Keep** the model default as `claude-sonnet-4-6` (`sop.ts:32`).
- ⚠️ P0-reviewer note: leave `confineProjectFile` (`ClaudeService.ts:110-115`, adjacent to the shapeParams deletion) for P1.
- **PR:** 1. **Verify:** tsc+eslint; selftest prints PASS; grep zero importers.

## P1 — Convergence ①: shared path confinement `[T1b, S5, S6]` — risk: med — depends: P0
- **T1b** add `export function confinePath(dir, rel): string|null` to ProjectStore (exact body from `:239-244`); refactor `resolveProjectFile` to call it. Delete the verbatim private `confineProjectFile` copies in `ClaudeService.ts:110-115` (call site 185) and `export.ts:38-43` (call site 105), import `confinePath`. ProjectStore is a leaf module → no cycle.
- **S6** (per red-team: per-site wrap is PRIMARY, not a normalizeSteps rewrite — `confinePath` returns *absolute*, but step paths are stored/consumed *relative*; rewriting would break `shot://` + manifest round-trip). Wrap each manifest-sourced join: `redactScan` (`ipc.ts:429`), `deleteSteps` (`ProjectStore.ts:524-526`). Optionally add a read-time **validator** in `normalizeSteps` that *nulls/drops* a step whose rel path escapes (keep the relative string when in-bounds).
- **S5** (per red-team: SEPARATE from S6 — `stepId`/`keepId` are NOT path fields, never pass through normalizeSteps). Add a UUID-shape check on `stepId`/`keepId` before the `.render` writes (`ProjectStore.ts:402,459`) or at the IPC layer.
- **PR:** 1 (theme: path confinement). **Verify:** tsc+eslint; live shot:// serve + Auto-redact + discard-session + delete-step; manual hand-edited `../` in `project.json` is refused; unit test `confinePath` fuzz vectors.

## P2 — Convergence ②: redaction backstop + egress pin `[S3, T1a, S10]` — risk: high — depends: P1
- **S3 + extraction** pull `updateStep`'s invalidation else-branch (`:408-417`, null `flattened`+`markerBaked` when `'annotations' in patch || 'crop' in patch` and no fresh PNG) into `applyPatchAndInvalidate(step, patch, hasFreshPng)`; call from `updateStep` **and** `mergeSteps` (`:439-474`, which currently has **no** else-branch).
- **T1a** create `src/main/render-gate.ts` exporting `resolveSendableRender(dir, step, stepLabel, verb) → {abs, mediaType, ext}` — the fail-closed gate (`hasBlur`; `rel=step.flattened`; throw if `!rel && (hasBlur||crop)`; `relToRead=rel ?? screenshot`; `abs=confinePath(...)`; throw if `!abs`; derive ext/mediaType). Replace `ClaudeService.ts:173-186` (verb `send`, label = `n` over `!aiInserted`-filtered source) and `export.ts:96-106` (verb `export`, label = `shotNo` over full steps; keep its extra `fs.stat` after). ⚠️ Caller passes its own precomputed `stepLabel` — the two use different numbering.
- **S10** `makeClient(apiKey) → new Anthropic({ apiKey, baseURL: 'https://api.anthropic.com' })`, use at all 3 sites (`ClaudeService.ts:84,239,271`). Batched here (same hot file).
- **PR:** 1 (highest-value security). **Verify:** tsc+eslint; **redaction fail-closed live test** (add blur→save→send/export show redacted; change blur WITHOUT re-save → both REFUSE); **direct-call test** of `applyPatchAndInvalidate` (merge else-branch is dead on every UI path — UI test proves nothing); real SOP gen; **negative test:** bogus `ANTHROPIC_BASE_URL` → all 3 sites still hit api.anthropic.com.

## P3 — Electron hardening `[S2, S4, S8, S9]` — risk: high — depends: P4
- **S2** decouple `--no-sandbox` from the HW-accel branch (`main.ts:96-105`): keep GPU switches under `SHOTAI_ENABLE_GPU!=='1'`, gate `--no-sandbox` behind its own `SHOTAI_NO_SANDBOX==='1'` so the packaged app ships sandbox-**ON**. Fix the misleading comment.
- **S4/S8** register `app.on('web-contents-created', …)` before `createWindows()`: `setWindowOpenHandler(()=>({action:'deny'}))`; `will-navigate` + `will-redirect` `preventDefault` unless local origin (`shot:`/`file:`/dev URL). (S8 = accepted residual under single-window + contextIsolation; this guard covers the navigation surface, NOT per-handler sender checks — state that explicitly.)
- **S9** `RegionService` `isOverlaySender(event)` guard at the top of both `ipcMain.on` handlers (`:30,33`); ignore non-overlay senders. ⚠️ depends on P4 (RegionService imports shared `parseRect` first — same lines, avoids conflict).
- **PR:** 1 (Electron theme). **Verify:** tsc+eslint; ⚠️ **sandbox-on cannot be verified on this ARM VM** — needs real x64 hardware or a packaged smoke-test (otherwise a renderer-fails-under-sandbox regression ships unseen); Area capture still works post-S9; `will-navigate` + `will-redirect` + `window.open` all denied; `npm run build`.

## P4 — Tier-3 dedup `[T3b, T3a]` — risk: low
- **T3b** add pure `parseRect`/`parsePoint` (+ local `isNum`) to `shared/project.ts` (next to Rect/Point; keep dependency-free). Delete locals in `ipc.ts:90-103` (import) and the private one in `RegionService.ts:38-51` (import; call site 31 → `parseRect(rect)`).
- **T3a** add `export function markerColorFor(step)` to `annotations.ts` (hoist `merge.ts:21-23` verbatim; export `RIGHT_CLICK_COLOR='#2563eb'` beside `ACCENT`). Replace inline copies in `merge.ts:21-23`, `Report.tsx:48-49`, `sop-prepare.ts:52`. ⚠️ **Do NOT touch `Editor.tsx`** — it seeds `markerColor ?? ACCENT` with no right-click branch; swapping would silently flip un-set right-click markers rose→blue.
- **PR:** 1. **Verify:** tsc+eslint; live Area capture + editor click-reposition + marker colors unchanged. **Lands before P3's S9.**

## P5 — Supply-chain + offline assets `[S1, S7]` — risk: med — depends: P3
- **S1** in `postinstall.mjs` `fetchNpmTarball`: load `package-lock.json`, compute `sha512` of the downloaded buffer, compare to `lock.packages[key].integrity`, throw on mismatch (augments the `buf.length<1024` check). Callers: node-screenshots (`:86-91`), koffi (`:107-112`). ⚠️ **residual:** `get-windows` downloads via `node-pre-gyp` (`:67-74`) and stays unverified — either extend a pinned-hash check or document as accepted residual.
- **S7** vendor `vendor/tessdata/eng.traineddata.gz`; append to `forge.config.ts` extraResource (wire resource FIRST); in `ocr.ts:42` pass `langPath` (packaged: `process.resourcesPath/tessdata`; dev: `app.getAppPath()/vendor/tessdata`) + `gzip:true`.
- **PR:** 1 (packaging-affecting). **Verify:** tsc+eslint; fresh install logs integrity-verified + a tampered lockfile-integrity char throws; first-run OCR works network-blocked; `npm run build`.

## P6 — SOP extraction + atomic writes `[T2d, T1c]` — risk: med — depends: P1, P2
- **T1c** route `writeManifest` (`:106-115`) **and** `createProject`'s inline write (`:169-173`) through `writeFileAtomic` (already imported by settings/secrets). Do **before** T2d.
- **T2d** add `export function mutate(projectPath, fn)` (writeQueue-serialized read-modify-write; keep `writeQueue`/`readManifest`/`writeManifest` private). Create `src/main/sop-apply.ts`; move `applySopEdits`, `revertSop`, `makeTextStep` out, rewriting bodies to call `mutate(...)`.
- ⚠️ **BLOCKER fix (red-team):** `ipc.ts:480` calls `projectStore.revertSop` — add `ipc.ts` to this phase and repoint it to `import { revertSop } from './sop-apply'`. (Also update `ClaudeService.ts:21` `applySopEdits` import.) Graph stays acyclic: `claude-service → sop-apply → project-store`.
- **PR:** 1. **Verify:** tsc+eslint; SOP generate/revert/regenerate-twice (first snapshot preserved); **direct concurrent-call test** for mutate serialization + atomic write (not non-deterministic "rapid capture"); grep no other `makeTextStep` importer.

## P7 — Module extraction `[T2a, T2b, T2c]` — risk: high — depends: P4
- **T2a** extract pure `capture-geometry.ts` (`unionRect`/`cropToRegion`/`clickBox`/`captureModeFor`) + `click-caption.ts` (`controlWord`/`buildClickCaption`) from CaptureController. **DEFER** the `MenuTracker` class (touches `this.natives`/timing; preserve same-arm guards `:549,578,584`).
- **T2b** extract `BlurRegion.tsx` (`Editor.tsx:111-192`, all-props) + `editor-geometry.ts` (`clampRectToImage:77-86`, optionally `computeCropView:286-306`). Don't extract `eventImagePoint`/`pointer` (read stageRef).
- **T2c** extract `Home.tsx` from `App.tsx` (JSX + picker state + row actions); Home owns picker via callbacks; App keeps `onRecord` + recording HUD. **Do NOT** lift picker state to the store.
- **PR:** 3 commits/PRs (one per extraction). **Verify:** tsc+eslint each; **golden-value before/after** for the pure geometry fns (`clickBox`/`cropToRegion` are redaction-adjacent — pixel drift = marker/crop misplacement); live capture (incl. right-click→menu), editor draw/resize/crop/save, Home create/open/rename/delete/export. ⚠️ don't claim to verify same-arm guards (not moved this phase).
- **STATUS 2026-07-06:** **T2a DONE** (`17e054f`) — `capture-geometry.ts` (`unionRect`/`clickBox`/`captureModeFor`/pure `cropRect`) + `click-caption.ts`, 21 golden-value tests; `clickBox` now takes a scale factor. **T2b DONE** (`d8ac3f9`) — `editor-geometry.ts` (`clampRectToImage`/`computeCropView`, 8 tests) + `BlurRegion.tsx`. Both pure moves, tsc/eslint/86 vitest green, pushed to `main`. **T2c DONE** (`98f03e9`) — extracted the self-contained projects LIST into `ProjectList.tsx` (App.tsx 781→580; 4-prop surface: projects/onOpen/onChanged/onError + its own useConfirm). Adversarial 2-reviewer pass + live smoke-test confirmed behavior-preserving. **The create form + capture-mode picker were LEFT in App on purpose** — the picker state feeds `buildTarget()`, which `onRecord` needs for BOTH create AND resume-from-detail; a Home that unmounts would silently change resume-capture's target (no test net). Extracting that half safely needs a lifted "last target" ref — deferred as optional.

## P8 — CSS + IPC tidy `[T3h, T3j]` — risk: med — independent
- **T3h** cut `.ed__*` block (`project.css:1500-1757`) into `src/renderer/editor/editor.css`; `import './editor.css'` in `editor/Editor.tsx` (or project `main.tsx`) — same change, or styles vanish.
- **T3j** (red-team: 2 ends, not 3 — `CaptureController.start` already takes opts) pass the `capture.start` opts object through: `preload.ts:106-112` send the object; `ipc.ts` handler receive an opts object. CaptureController unchanged.
- **PR:** 1 (2 commits). **Verify:** tsc+eslint; editor styles render identically; capture for both fresh-this-session and existing project (createdThisSession preserved).

## P9 — Function-bag kebab-renames `[T3g]` — risk: low — depends: P1, P2, P5, P6 (LAST)
- `git mv` `ProjectStore.ts→project-store.ts`, `ClaudeService.ts→claude-service.ts`, `ElementLocator.ts→element-locator.ts`, `atomicWrite.ts→atomic-write.ts`. Keep `CaptureController`/`RegionService` (real classes).
- Update import specifiers (ipc, main, sop-apply, render-gate, claude-service, settings, secrets, + any tsc surfaces). ONE commit. No renderer file affected (talks to main only via IPC).
- **PR:** 1, isolated mechanical. **Verify:** tsc+eslint; `npm run build`; smoke `npm start`.

## Deferred
- **T3i** regroup flat `src/main/` into feature folders — low-payoff/medium-churn; would re-touch every import right after P9. Revisit if `src/main/` grows.
- **T3k** move `flatten.ts`/`annotations.ts` to a `render/` dir — optional; must land after T3a/T3b/T3c/T2b to avoid double-editing import sites. Standalone follow-up if desired.

## Coverage
S1→P5, S2→P3, S3→P2, S4→P3, S5→P1, S6→P1, S7→P5, S8→P3, S9→P3, S10→P2 · T1a→P2, T1b→P1, T1c→P6 · T2a/b/c→P7, T2d→P6 · T3a/b→P4, T3c/d/e/f→P0, T3g→P9, T3h/j→P8, T3i/k→deferred · DOC→P0.

## Status (branch `hardening`, as of 2026-06-30)
- [x] P0.5 vitest · [x] P0 · [x] P1 · [x] P2 · [x] P3 · [x] P4 · [x] P5 (S1 + S7) · [x] P6 · [x] P8 · [x] P9
- [x] **P5/S7** — vendored `vendor/tessdata/eng.traineddata.gz` (2.82 MB LSTM `best_int`) + forge extraResource → `resources/tessdata` + `ocr.ts` langPath; offline OCR reads the local model (no CDN) — confirmed in dev AND in the packaged app on x64 (see S7 + S11).
- [ ] **P7** (split CaptureController / Editor / App→Home) — DEFERRED: behavior-preserving but needs live capture/editor/home testing; to be done together.

Verified for the landed phases: `tsc --noEmit` + eslint clean (only the pre-existing CaptureController non-null warning), **36** vitest tests green (added `mutate-serialize.test.ts`), selftest PASS. **All pending hardware/live checks VERIFIED on real x64 hardware 2026-06-30:** S2 sandbox-ON launch, S3/T1a live redaction fail-closed (+ 0-bypass adversarial audit), S10 egress pin, P6 SOP apply/revert + mutate serialization, S7 packaged offline OCR. **Packaging blocker S11 found + fixed** (the Vite-packaged app shipped no `node_modules`; added a `packageAfterCopy` closure-copy hook). New post-testing UX backlog recorded (E1–E6). Still worth before release: `npm run make` full-installer smoke test; P7 extraction.

## Testing on a physical x64 Windows machine

This is **much simpler than the arm64 dev VM** — no Rust/MSVC/Python needed (the `element_locator.dll` is committed; native deps are x64 prebuilts npm installs directly), and none of the `SHOTAI_NO_SANDBOX` / arm64-repair steps apply. Prereqs: **Node 24 LTS + Git**.

```sh
git clone https://github.com/Armadillon44/shotAI.git
cd shotAI && git checkout hardening
npm install        # installs x64 binaries natively; if koffi/get-windows don't load: node scripts/postinstall.mjs
npm start          # real Electron + real main.ts → OS sandbox ON (no env vars) = the shipped default
```

Three levels of testing, easiest first:
1. **`npm start`** (dev) — fastest; verifies the sandbox-on default + lets you run all the live UI checks. (GPU is disabled by default even here — a VM workaround that currently ships to all users; `set SHOTAI_ENABLE_GPU=1 && npm start` to see HW accel. Doesn't affect the sandbox check.)
2. **`npm run package`** then run `.\out\shotAI-win32-x64\shotAI.exe` — the real **packaged** app (asar, `resources/tessdata`, `.ico`, fuses, sandbox) without needing the Squirrel installer. Best single check for S7's packaging path + the exe icon.
3. **`npm run make`** — full Squirrel installer (`out\make\squirrel.windows\x64\*Setup.exe`). Never run end-to-end; `electron-winstaller` will likely need an npm-11 allow-scripts approval first. Do last, for release.

Notes: don't copy `node_modules` from the arm64 VM (install fresh on x64); paste a Claude API key into Settings for the SOP checks.

### Pending-verification checklist (do on the x64 machine)
- [x] **S2 — sandbox on**: `npm start` with NO env vars; confirm the window paints + renderers run (i.e. the OS sandbox doesn't break startup on real hardware). **VERIFIED 2026-06-30 (x64)** — no `OS sandbox DISABLED` warning (sandbox ON by default), both `toolbar window loaded` + `project window loaded`, 4 sandboxed electron procs, `listTargets: 2 windows, 1 monitors` (native capture binaries load).
- [x] **S3 / T1a — redaction fail-closed**: open a step, add a blur, **Save** (bakes the render); confirm SOP-send + all export formats show the redacted image. Then add/change a blur or crop and **don't** re-save → both SOP-send and export must **refuse** (not emit raw pixels). **VERIFIED 2026-06-30 (x64).** Happy path: blur+Save co-baked a fresh `flattened` (renderRev bumped); pixel-diff proved the blur baked into the render (38.9% of the blur region changed vs 0% control; visually pixelated). Refuse path: hand-edited `project.json` to `flattened:null` keeping the blur (normal UI can't reach the unbaked state — Editor Save + capture merge always co-bake), then in-project **Export→HTML refused live** with *"Step 2 … hasn't been baked into a render yet — refusing to export the raw screenshot"* — no `exported→` log line, no artifact written. The two-layer design held: renderer `ensureFlattened` re-baked the 4 redaction-free unbaked steps, skipped the stale-store blurred step, and the main gate caught the tampered disk state. Both egress paths call the gate (`claude-service.ts:170` send, `export.ts:92` export); send verb is unit-tested too. Unit tests green on x64: `render-gate.test.ts` 5, `step-render.test.ts` 4. **Adversarial audit (5-lens workflow, 113 tool calls): 0 image-egress bypasses** — send + export are the only paths.
- [x] **S10 — egress pin**: set `ANTHROPIC_BASE_URL` to a junk host (e.g. `http://127.0.0.1:1`), run a SOP generate + key test + estimate; all must still reach api.anthropic.com (not the junk host). **VERIFIED 2026-06-30 (x64, mechanism)** — with junk env set, shotAI's exact construction resolves `client.baseURL=https://api.anthropic.com`; control (unpinned client) picked up the junk host → pin does real work. All 3 sites route via `makeClient` (`claude-service.ts:93,220,252`). Live end-to-end (junk env + real key → auth error from real host, not ECONNREFUSED) still optional.
- [x] **P6 — SOP apply/revert + atomic writes**: generate a SOP, Revert (restores the pristine pre-AI snapshot), regenerate twice (first snapshot preserved, no compounding); capture rapidly while editing to sanity-check serialization. **VERIFIED 2026-06-30 (x64).** Live apply/revert on "win x64 test 001" (6 shot steps, model `claude-sonnet-4-6`): generate #1 created `sopBackup` = pristine 6-step snapshot + original title (snapshot `aiInserted=0`), inserted 3 AI text steps (6→9), AI-refined the title. Generate #2 with NO revert: `sopBackup` preserved **identically** (same `at` timestamp — not re-snapshotted), steps stayed 9 (not 12) and `aiInserted` stayed 3 (not 6) — no compounding (`!aiInserted` base-rebuild + `?? {}` snapshot-preserve both hold). Revert restored the **exact** baseline (step IDs identical in order, title back to original, `sopBackup` cleared to null). Serialization done as a **direct concurrent-call test** (plan's preferred approach over flaky rapid-capture): new `src/main/mutate-serialize.test.ts` (3 tests, green) drives the REAL `mutate` — 40 concurrent RMW mutations all land (no lost updates, file never torn), no stray `.tmp`, and a throwing mutation aborts its own write without corrupting/losing others. Full suite now 36 green on x64.
- [x] **S7 / packaging** (via `npm run package`): first-run **Auto-redact** OCR works with the network off (reads the vendored `resources/tessdata` model); the packaged `.exe` shows the shotAI icon. **VERIFIED 2026-06-30 (x64)** — `npm run package` clean; `shotAI.exe` carries the shotAI capture+sparkle icon (not default Electron); `resources/tessdata/eng.traineddata.gz` (2.82 MB) bundled. After the **S11** packaging fix the packaged app launches (`packaged=true`, capture/IPC working); with **network OFF** and the userData OCR cache cleared, Auto-redact logged `initializing OCR worker (vendored lang: …\out\shotAI-win32-x64\resources\tessdata)` and `auto-redact: 18 line(s), 0 sensitive region(s)` — OCR read 18 lines from the **bundled** model with zero network/CDN access (cache stayed empty → read straight from `langPath`).

### ✅ S11 — Packaged app is DOA: `node_modules` not shipped (found + FIXED 2026-06-30, x64) — was a RELEASE BLOCKER
**FIXED:** added a `packageAfterCopy` hook in `forge.config.ts` (`copyProductionNodeModules`) that walks the **actual runtime dependency closure** (each `package.json`'s `dependencies` + `optionalDependencies`, BFS from the root's prod deps) and copies each package dir recursively into the packaged app's `node_modules`. Chose closure-walking over a `package-lock.json` `dev`/`devOptional` filter because npm flags some genuinely-needed transitive deps as `devOptional` (e.g. `readable-stream`, reached via `get-windows → @mapbox/node-pre-gyp → npmlog → are-we-there-yet`), which a flag filter wrongly dropped (caused a second launch crash `Cannot find module 'readable-stream'`). Result: 99 prod packages copied → 153 in the asar incl. the full node-pre-gyp chain; `.node` binaries unpacked via `asar.unpack`. Packaged `shotAI.exe` now launches (`packaged=true`), capture/SOP/offline-OCR all work. Static gates still green (tsc, eslint, 36 vitest). ⚠️ Two iterations were needed (electron-log, then readable-stream); a `npm run make` full-installer smoke test is still worth doing before release. Original diagnosis below.


Running `out\shotAI-win32-x64\shotAI.exe` crashes immediately with an Electron error dialog:
`Uncaught Exception: Error: Cannot find module 'electron-log/main'` (require stack `app.asar\.vite\build\main-*.js`).
**Root cause:** `app.asar` contains only `.vite/` + `package.json` — **zero `node_modules`** (verified via `asar list`; also no `app.asar.unpacked/`). The Vite main/preload builds **externalize** electron-log, the native modules (`uiohook-napi`, `node-screenshots`, `get-windows`, `koffi` + `@koromix/koffi-win32-x64`, `node-screenshots-win32-x64-msvc`), `tesseract.js`, `@anthropic-ai/sdk`, and `zod` (see `vite.main.config.ts`), expecting them at runtime — but `@electron-forge/plugin-vite` packages only the Vite output and does **not** copy `node_modules`, even though the packaged `package.json` declares them under `dependencies`. The app dies on the first external require (`electron-log/main`); every other externalized module (capture, hotkeys, element-locator, OCR, Claude) would fail too. Dev (`npm start`) works only because it runs from the repo's `node_modules`. This is **pre-existing** (not introduced by the hardening phases) and is exactly the gap the "`npm run make` packaging path still untested" note flagged.
**Fix options:** (A) for native/file-based externals (`uiohook-napi`, `node-screenshots`, `get-windows`, `koffi`, `tesseract.js`) keep them external and **copy their `node_modules` (incl. transitive + the optional `*-win32-x64*` binary packages) into the package** via a forge `packageAfterCopy` hook (or fix forge's prod-dep copy); (B) additionally **bundle the pure-JS externals** (`electron-log`, `@anthropic-ai/sdk`, `zod`) into `main.js` by removing them from `vite.main.config.ts` external (bundling the SDK+zod **together** also resolves the dual-zod-instance concern that motivated externalizing them). Re-package and confirm `app.asar` contains the needed `node_modules`, the app launches, capture + SOP + offline OCR all work. ⚠️ Verify on this x64 box — packaging-path-only failures don't show in dev.

---

## Post-testing UX backlog (new — reported 2026-06-30, fix AFTER the verification checklist)

> **Status:** E3/E5/E6 (hovering translucent dismissable error notices) **DONE** (`eafc15a`). E1/E2 landed as an interim props-bar text form, then **E7 (true inline on-canvas text editing) DONE 2026-07-01** — superseded the props-bar form: click the image and type directly where the text lands (overlay input), size/color in the edit bar, dblclick/Edit-text to re-edit in place, empty texts dropped. Fixed a focus-steal where the creating click's mouse-up blurred the just-opened input (opening-guard absorbs that one blur). **Still pending:** E4 (SOP intro preamble), C1 (GPU default), **P7** (module extraction). All static gates green (tsc/eslint/36 vitest).
UX issues across the editor, SOP pipeline, and project view — distinct from the security/structure hardening above. Several editor items (E1–E3) share a root cause: transient controls/messages are inserted as **block elements in the `ed__main` column above `ed__canvas`**, so each one pushes the image down and squishes the view. The error-banner items (E3, E5) are the same family (`project__error` / editor `error` — persistent, non-dismissable block banners).

- **E1 — Bug: can't edit existing text.** Editing an existing text element opens via `onDblClick` on the `<KText>` node (`Editor.tsx:1137`) → `setTextEntry({ id: a.id, value: a.text, … })`; commit goes through `commitText` (`:585-600`, `update(entry.id, { text, fontSize })`). Double-click isn't reliably reopening the entry — likely the text node only receives pointer events under `tool==='select'` (the `common` props gate interaction), or the dblclick is swallowed. Fix: make editing reachable when a text element is selected (e.g. an explicit "Edit text" affordance in the element header per E2, and/or fix the dblclick listening condition). Verify: add text → save → reopen step → edit the text → save → text updates.
- **E2 — Bug: text add/edit should live in the element header, not a stacked banner.** The text entry renders as a full-width `<div className="ed__textbar">` (`:938-986`) inserted above the canvas, adding yet another banner that squishes the image. Move the add/edit-text field + size + commit into the contextual element header / props area (`ed__topbar` `:786` or `ed__props` `:838`) so it doesn't add a row. (Ties into E1: a header-based edit affordance also fixes the "can't reopen" gap.)
- **E3 — Improvement: OCR "no matches" (and other) errors squish the image.** `autoRedact` sets `setError('No sensitive data detected …')` (`:639`); `error` renders as a block `<p className="project__error">` above the canvas (`:936`), squishing the view. Make it an absolutely-positioned **overlay hovering at the top of `ed__canvas`** with a translucent/opacity background + a **dismiss (×)** button (`setError(null)`). Note `error` is also the sink for save/autoRedact *failures*, so the overlay must read well for both the benign "no matches" notice and real errors (consider distinct styling/severity).
- **E4 — Bug: the SOP "Overview"/intro should not be a discrete step.** `applySopEdits` pushes `plan.intro` as a discrete numbered text step at the top (`sop-apply.ts:64-66`, `makeTextStep(intro.heading, intro.body, aiInserted=true)`; schema `shared/sop.ts:129-130`), so the overview shows up as step [0] in the report/export and is counted/numbered like a real step. It should render as a **non-step preamble/header** above the steps instead. Likely fix: store the intro on the manifest (e.g. `manifest.intro`) rather than as a step, render it as a lead-in in `Report.tsx`/export, and drop it from the numbered-step flow (and from `revertSop`'s step list). Cross-check: `sentSteps = steps.filter(!aiInserted)` (SopPanel:60) and the `!aiInserted` base-rebuild in `applySopEdits` must still behave once the intro is no longer a step. ⚠️ section headings (also `aiInserted` text steps, `:80`) are a separate decision — the user flagged only the **overview/intro**, so don't change section-heading behavior unless asked.
- **E5 — Bug: the Export error persists and isn't dismissable.** `ProjectDetail.tsx:319` renders `{exportErr && <p className="project__error">Export failed: {exportErr}</p>}` — a persistent block banner with no dismiss; it only clears on the next export attempt (`:100`). Same for sibling banners `importErr` (`:318`) and `error` (`:320`). Add a **dismiss (×)** that clears the state (`setExportErr(null)`), and consider auto-clearing on navigation. Same error-banner family as E3 — ideally a shared dismissable-notice component for both the editor and project views. (Surfaced during S3 testing: the fail-closed refuse error stuck around with no way to dismiss it.)
- **E6 — Bug/Improvement (umbrella): ALL error messages should hover + be translucent, not inline.** Across the app, errors render as **inline block elements** that push every other UI element down (editor `error` `Editor.tsx:936`; project `importErr`/`exportErr`/`error` `ProjectDetail.tsx:318-320`; and any other `project__error`). They should instead be **absolutely-positioned, translucent, dismissable overlays hovering over their region** so the layout never shifts. This generalizes E3 + E5 — implement ONE shared `<Notice>`/toast component (severity-aware, auto-stack, click-to-dismiss, optional auto-timeout) and route all error/notice state through it. (Reported 2026-06-30 while testing the packaged app — startup IPC errors appeared as layout-shifting inline banners.) **DONE** — `src/renderer/Notice.tsx` + `notice.css`, wired into the editor, project detail, and home. SopPanel + Settings keep contextual inline errors (follow-up). Made translucent via alpha (0.6) + text-shadow because the GPU is disabled so `backdrop-filter` blur is a no-op (see C1).
- **E7 — Text tool: true inline on-canvas editing (supersedes the E2 props-bar form).** Requested 2026-06-30. The desired UX: pick the Text tool, click the image, and **type directly on the canvas at that spot — no separate text field**. The element header (`.ed__props`) keeps **size + color** controls for the active text. Selecting an existing text later makes it **editable in place** (no modal). Standard implementation: overlay an HTML `<textarea>`/`<input>` on the Konva stage at the text's screen position — `screenX = stage.container().getBoundingClientRect().left + (ix − region.x)·cropView.scale` (mirror for Y), font-size = `fontSize·cropView.scale`, transparent background, color = `fill`; create the text annotation on click and update it live; commit on Enter/blur (drop if empty), Escape cancels; hide the underlying `KText` while its textarea is open; recompute position on zoom/scroll/crop. Replace the current `textEntry` props-bar form + `commitText` + the `{textEntry && <KText … 'Text…'>}` preview with this. Files: `src/renderer/editor/Editor.tsx` (+ a small `.ed__textedit` style). ⚠️ Get overlay positioning right under zoom/pan/crop — needs live iteration.

## Session-2 backlog (2026-07-01)
- **M1 — DONE 2026-07-01.** SOP model default → `claude-sonnet-5` (latest Sonnet; `SopModelId` narrowed to just it, so a saved `claude-sonnet-4-6` auto-migrates via `coerceSopSettings`); **removed the Opus option**; added a user-configurable **effort** setting (`SopEffort` low/medium/high, default medium) in Settings, wired to `output_config.effort` (model declares `supportsEffort`). Verified live: generation succeeds on Sonnet 5.
- **E8 — Author/edit your own SOP intro.** Requested 2026-07-01. Make the `manifest.intro` preamble (E4) **user-editable** in the report: click-to-edit an existing intro + an "Add intro" affordance when there is none, independent of running a generation. Needs a `setProjectIntro(projectPath, intro|null)` IPC → `mutate()` sets `manifest.intro`, a store action, and Report UI (reuse the `TextStepEditor` heading/body pattern). Doubles as the live confirmation of E4's preamble render (Claude doesn't always emit an intro).
- **E9 — Convert a text-only step to a callout.** Requested 2026-07-01. Add an action (report text-step controls / overflow) to turn a plain text step into a callout (note/caution/warning) and back — set/clear the `callout` (`CalloutKind`) field via `updateStep`. See `Report.tsx` callout rendering + `addTextStep`/`CalloutKind`.
- **E10 — Callouts should not be numbered steps.** Requested 2026-07-01. Callout text steps currently take a numbered rail position; render them un-numbered (like the E4 preamble) in the report AND exports. See `Report.tsx` `rail()`/`rep__num` (text uses `rep__num--text`) and `export.ts` `## N.` numbering. Decide callouts-only vs all-text-steps (user flagged callouts).
- **E8 — DONE 2026-07-01** (author/edit your own intro): `setProjectIntro` IPC → `mutate` → `coerceIntro`; preload + Report UI (Add overview / Edit / Remove, reusing a generalized `TextStepEditor` with `initialHeading`/`initialBody`). Verified working. Follow-up fix landed same session: the intro/text editor field was narrow + too tall → `.rep__textedit-h/-b` now `width:100%` box-sizing, textarea `rows=3` + `min-height 3.25rem`.
- **B1 — BUG: exporting more than once produces no file; serialize duplicate export names.** Reported 2026-07-01. `export.ts` writes to `export/<base>.<ext>` (e.g. `<base>.html`, `<base>-plain.html`, `<base>.pdf`, markdown dir). A second export appears to not produce a file — likely the fixed path fails/overwrites while the previous file is open/locked, or the reveal no-ops. Fix: when the target exists, serialize the name (`<base> (1).html`, `(2)`, …) — like `safeFileBase` but with a uniqueness suffix — for every format (incl. the markdown `<base>/` image dir). Verify: export twice → two files; export while the first is open → still succeeds.
- **B2 — BUG: export does not honor a step's zoom.** Reported 2026-07-01. Steps carry a per-step `reportZoom` (see `Report.tsx` updateStep `{reportZoom}`) that zooms the image in the in-app report, but exports embed the full flattened render, ignoring it. Fix: apply `reportZoom` when building the export image (crop/scale the flattened PNG to the zoomed view) across HTML/PDF/Markdown, matching what the report shows. ⚠️ redaction-adjacent — the zoomed export must still come from the baked `flattened` render (never the raw screenshot); keep the fail-closed gate.
- **UX7 — TODO: remove the redundant "Import Image" button in the editor.** Reported 2026-07-01. Drop the import-image affordance in the SOP editor (redundant). Find it in `Editor.tsx`/`ProjectDetail.tsx` (`importStep`/`onImportFile` + the file input) and remove the button (keep or prune the IPC if now unused).
- **UX8 — TODO: float the editor zoom + delete-step controls over the image.** Reported 2026-07-01. Move the editor's zoom controls and the delete-step/element control into a **translucent, vertical cluster pinned to the top-right corner over the canvas** (like the E6 notice overlay), instead of the toolbar row. `Editor.tsx` `.ed__zoom` + the props-bar Delete button; overlay in `.ed__canvaswrap` (already position:relative).
- **UX9 — capture mode that doesn't hide the app window.** Reported 2026-07-01. During capture, main hides the project window + shows the pill. **DONE (env var) 2026-07-01**, then **promoted to a persisted Settings toggle 2026-07-06** ("Keep shotAI visible during capture" — `settings.json` `captureNoHide`, read synchronously via `captureNoHideNow()` at recording-start; the `SHOTAI_CAPTURE_NO_HIDE` env var was removed). The toggle keeps the window VISIBLE only — capture still ignores clicks on shotAI's own windows (`OWN_WINDOW_TITLES`) and shows a read-only recording HUD, so shotAI itself isn't captured/interactive during a recording. **DEFERRED (user, 2026-07-06):** whether "demo mode" should additionally let recordings capture shotAI's own window or allow live interaction — larger design question, not yet decided.

## Config / behavior backlog (new — 2026-06-30)
- **C1 — GPU enabled by DEFAULT (with a safe fallback).** Requested 2026-06-30. Today `main.ts:96` disables HW acceleration unless `SHOTAI_ENABLE_GPU==='1'` (a VM workaround that currently ships to ALL users → software rendering, which also makes CSS `backdrop-filter` a no-op, see E6). Flip the default to **GPU ON**, and only disable it when we **detect** it won't work — e.g. a virtual machine or an arm64 host running x64 under emulation (the contexts where GPU-context creation aborts startup, as seen by the `Failed to create shared context for virtualization` GPU errors on the dev VM). Keep an env override for manual force on/off. Detection ideas: `app.getGPUFeatureStatus()` / `app.getGPUInfo()` after `ready`, CPU arch vs. process arch mismatch (arm64 host + x64 emulation), or a known-VM heuristic; fall back to `disableHardwareAcceleration()` + the GPU switches only then. Verify on real x64 HW (GPU on) AND the arm64 dev VM (must still launch). Bonus: with GPU on, the E6 notice `backdrop-filter` blur starts working.
  - **C1 — DONE 2026-07-06.** Pure `src/main/gpu-policy.ts` (`decideGpu(env, platform, arch)` + `isEmulatedOnArm`, 12 unit tests). Default GPU ON; auto-disable ONLY on x64-under-ARM64 emulation, detected synchronously before app-ready via `PROCESSOR_ARCHITEW6432` (requires native arch ≠ x86, so 32-bit WOW64 isn't misflagged). `SHOTAI_ENABLE_GPU` now tri-state (`1`=on, `0`=off, unset=auto); fail-open to ON on detection error. main.ts applies it + logs a post-init GPU summary. **Verified on physical x64 (Intel Arc Pro): launches with GPU on, `gpu_compositing=enabled`/`webgl=enabled`/`2d_canvas=enabled` (HW). NB: `getGPUFeatureStatus()` at `whenReady` is premature (reports software defaults before the GPU process finishes probing) — read it after `getGPUInfo()` resolves or on `gpu-info-update`, else you'll misread a working GPU as software.** ARM-emulation branch covered by unit tests (couldn't re-test the VM this session).

## Session-2 batch 2 — DONE 2026-07-01 (branch `hardening`)
Reported after the UX8 test pass. All static gates green (tsc + eslint + 45 vitest); pending live x64 verification. Each backed by an adversarial multi-agent diagnosis workflow.
- **E9/E10 — DONE.** Convert text↔callout via the ⋯ menu; callouts render un-numbered (type glyph on the rail) with contiguous numbering over non-callout steps; reorder-by-number maps display→absolute slot. `isCalloutKind` added to shared; `StepPatch.callout` widened to `CalloutKind|null` (null clears); `parseStepPatch` handles it.
- **B1/B2/UX7/UX8 — DONE.** (B1 serialize export names; B2 export honors per-step zoom via `export-geometry.ts` `zoomCropRect` + `nativeImage.crop`; UX7 removed Import button; UX8 floated zoom+delete over the image in `rep__figbox`/`rep__imgctl`.)
- **B3 — editor image pulses endlessly on zoom.** ResizeObserver↔scrollbar feedback loop (`.ed__canvas` `overflow:auto` → scrollbar toggles clientWidth → viewport → stage size → scrollbar…). Fixed with `scrollbar-gutter: stable both-edges` on `.ed__canvas`.
- **T1 — editor image fills width by default.** `Editor.tsx` fit changed from contain-fit `min(w/natW, h/natH, 1)` to width-fit `viewport.w/natW`; `.ed__canvas` `align-items: safe flex-start` (top-anchor, scroll down).
- **B4 — can't edit callouts after a delete following SOP-gen.** `editingTextId` is the single gate for opening ANY text/callout editor; it latched on a surviving id (author callouts keep their id across SOP-gen/delete, so the old id-vanish check never fired). Fixed with a store `manifestRev` (bumped on every manifest swap) + a Report effect keyed on `[manifestRev, steps]` that drops stale edit latches while preserving a genuine fresh-add draft.
- **B5 — can't change SOP effort in Settings.** `parseSopPatch` (ipc.ts) whitelisted only enabled/model/tone/customInstructions and silently dropped `effort` (added in M1). Fixed: `if (isSopEffort(v.effort)) patch.effort = v.effort`.
- **C2 — "Preparing and Estimating AI Cost" translucent top overlay + Cancel.** Replaced the inline `sopbar__prep` span with a portal overlay (`.sop__overlay--top` + `.sop__prepcard`). New `claude:cancel` IPC (send) → `cancelClaude()` aborts an `AbortController` wired into `client.messages.countTokens`/`.stream` `{ signal }`; renderer also aborts the flatten controller and swallows the cancel via `canceledRef`.
- **C3 — report canvas +20px.** `REPORT_BASE_W` 800→820 and `.rep__bodywrap` max-width 800→820 (kept coupled).
- **T2 — modest screenshot downscale.** `CaptureController.downscalePng` (nativeImage, `CAPTURE_SCALE=0.85`, fails open) shrinks the stored PNG; `click.image` scaled by the actual factor + a `click.imageScale` persisted; `merge.ts` recovers the capture origin using it; `parseClick` preserves it across editor saves. Downstream (report/editor/export/flatten) self-adjust to the stored dims. ⚠️ needs a live capture + right-click-menu **merge** test + a redaction-after-downscale check on x64.
- **Q1 (question, no code) — stream SOP live?** Answered: technically possible (SDK already streams; the code already receives text deltas) but the whole-document JSON structured-output shape + adaptive-thinking latency make a true typewriter impractical; the achievable UX is streamed thinking/status + per-step captions parsed from partial JSON, applied once at the end. Not implemented.

## Session-3 backlog (2026-07-06)
- **D1 — Image quality/downscale slider (with a readability floor). ✅ DONE 2026-07-06.** Promoted T2's hardcoded `CAPTURE_SCALE=0.85` to a persisted `captureScale` setting (`shared/project.ts` `CAPTURE_SCALE_MIN=0.5`/`MAX=1`/`DEFAULT=0.85`), clamped in `settings.ts` (`clampCaptureScale`) with a synchronous cache (`captureScaleNow`/`getCaptureScale`/`setCaptureScale`) primed at startup, and read per-capture by `CaptureController.downscalePng`. **Readability floor** implemented as `MIN_CAPTURE_LONG_EDGE = 1100`: `target = max(captureScaleNow(), min(1, 1100/longEdge))`, never upscales, fails open to the original PNG on any error. Slider UI is a "Screenshot quality" `settings__group` (range `[0.5,1]` step 0.05, live `onChange`, persist on `onPointerUp`/`onKeyUp`, `%` readout). IPC get/set + preload + main handlers wired; `click.imageScale` already per-shot so the slider only affects NEW captures. Original design below.
  - _Original design:_ Promote T2's hardcoded `CAPTURE_SCALE=0.85` to a user setting exposed as a **slider** in Settings. Design: persist `captureScale: number` in `settings.json` (coerced/clamped to `[FLOOR, 1]`, default 0.85), mirror it into a synchronous cache like `captureNoHide` (`captureScaleNow()`), and have `CaptureController.downscalePng` read it per capture. **Readability floor:** clamp so text stays legible to Claude — a scale floor (e.g. 0.6) AND/OR an absolute long-edge floor (never shrink the longer edge below ~1000px), whichever is higher, so small captures aren't over-shrunk. Slider UX: labelled "Screenshot quality" (Smaller files ↔ Sharper), show the % and a hint that lower = cheaper AI + smaller files but softer text. IPC get/set + Settings control. NB: `click.imageScale` is already persisted per-shot, so changing the slider only affects NEW captures; old shots keep their scale (merge stays correct).
- **D2 — Tabbed Settings. ✅ DONE 2026-07-06 (shipped in v1.0.0-rc2, PR #10 / merge 413da53).** Settings.tsx reorganized into a WAI-ARIA tabbed panel — **AI** (enable + key + model + tone + effort + custom instructions), **Capture** (quality slider + keep-visible toggle), **Storage** (projects folder), **About** — with `SETTINGS_TABS` + `tab` state, a `role="tablist"` bar (roving tabindex, Arrow/Home/End nav, focus-follow), a `role="tabpanel"` (tabIndex=0) that renders only the active section, and `aria-controls` only on the active tab. Pure renderer refactor (no IPC/settings changes); every control kept its handler verbatim. CSS: underline tab bar + flex-column panel (gap owns the rhythm). Adversarial review + live test found & fixed: dangling aria-controls, non-focusable tabpanel, off-note double-spacing, focus-ring clip, and a tab-bar vertical-scroll from `overflow-x:auto`. Original design below.
  - _Original design:_ Settings.tsx is getting unwieldy (AI on/off, API key, model, tone, effort, custom instructions, demo toggle, projects folder, about — soon the D1 slider). Break it into tabbed sections, e.g. **AI** (enable + key + model + tone + effort + custom instructions), **Capture** (demo mode + D1 quality slider), **Storage** (projects folder), **About**. Implementation: a `tab` state + a tab bar; render the active section; keep the existing load/save-per-change logic (each control still persists immediately). Pure renderer refactor — no IPC/settings changes. Do D2 around the same time as D1 so the new slider lands in a tidy Capture tab.
- **D3 — Native Microsoft Word (.docx) export.** Requested 2026-07-06. Add `'docx'` to `ExportFormat` and a real Word document builder (today's "HTML (for Word)" `html-plain` is a paste-into-Word stopgap; the user wants a genuine `.docx`). **Feasible & offline-clean:** the `docx` npm package (pure JS, no native deps, no CDN — bundles fine in the main process) builds `.docx` from `Document`/`Paragraph`/`ImageRun`, embedding images directly from a `Buffer`. Architecture already fits: `collectSteps` yields redaction-gated `ExportItem[]` (each shot has `abs` on disk or cropped `bytes` + `mediaType`; text/callout/caption/body/note as strings), so a `renderDocx(items, meta)` slots in beside `renderHtml`/`renderMarkdown` behind the SAME fail-closed collector — **no new path can read a raw screenshot**. Map: title→`HeadingLevel.TITLE`, intro/section→heading+paragraph, shot→numbered heading + `ImageRun` (size to a max width, preserve aspect from PNG dims) + instruction/note paragraphs, callouts→shaded/bordered paragraph (approximate the report's note/caution/warning colors via paragraph shading + left border). Write via `Packer.toBuffer` → `fs.writeFile` into the project `export/` folder + `nextAvailableStem`, then `shell.showItemInFolder`. ⚠️ verify image bytes come from the sendable render (`resolveSendableRender`), never `step.screenshot`, and that a cropped/zoomed step exports its cropped `bytes` (same as HTML). Est. medium; main risk is `docx` bundle size + getting image aspect ratios right.
- **D4 — Native Microsoft PowerPoint (.pptx) export.** Requested 2026-07-06. Add `'pptx'` to `ExportFormat` — one slide per step, good for training decks / screen-shares. **Feasible & offline-clean:** `pptxgenjs` (pure JS, runs in Node/Electron main, no CDN; embeds images as base64 data URIs — which `collectSteps`/HTML already produce via `data:${mediaType};base64,…`). Architecture: a `renderPptx(items, meta)` beside the others, same fail-closed collector. Layout per slide: title slide (project title + optional intro), then per shot step a slide with the caption as the title, the screenshot as a centered/contained image (scale to fit the slide's content box preserving aspect from PNG dims), and the instruction/note as body text or speaker notes; callouts → their own accent-colored slide or a colored text box. Save via `pptx.write({ outputType: 'nodebuffer' })` → `fs.writeFile` into `export/` + `nextAvailableStem` → `shell.showItemInFolder`. ⚠️ same redaction check as D3 (images strictly from the sendable render); watch aspect-ratio math so wide screenshots don't distort. Est. medium; do D3 first (shares the image-sizing/aspect helper and the ExportFormat plumbing pattern).
